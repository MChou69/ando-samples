package mc.com.tools

import android.content.Context
import android.content.Intent

/*
internal object Intents {
    fun OpenActivity(activityClass:Class<*>, context:Context) {
        val i = Intent(context, activityClass)
        context.startActivity(i)
    }
}*/
class Foo {
    companion object {
        fun a() : Int = 1
    }
}